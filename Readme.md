# Recon Suite (Docker + Bash)

All-in-one recon toolkit using Docker with tools like:
- nmap, curl, subfinder, httpx, dirsearch, nuclei, naabu

## 🐳 Setup

```bash
./setup.sh
```

## 🚀 Run Scan

```bash
./scan.sh example.com
```

Results will be saved in your current directory.
